#include <gtest/gtest.h>

#include <point.h>

TEST(point_suite, starting_point)
{
    const point p;
}

TEST(point_suite, implementation_in_cpp_file)
{
    const point p;
    p.dummy_method_in_cpp_file();
}
